#pragma once

#include <string>

// Преобразование инфиксного выражения в постфиксное по алгоритму Дийкстры
// (сортировочная станция). Числа предполагаются однозначными (одна цифра),
// поддерживаются операторы + - * / ^ и круглые скобки.
// Результат — токены постфиксной записи, разделённые пробелом.
std::string infixToPostfix(const std::string& infix);
